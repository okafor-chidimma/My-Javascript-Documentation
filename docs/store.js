/*
a store is an object containing all the states(default reducer states values)
you can use combineReducer even if there is just one reducer
combineReducer takes all those default state values and adds them to the store i.e a object with propName:value pairs

*/